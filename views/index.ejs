<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mini whatsapp</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>All Chats!</h1>

    <form action="/chats/new" method="get">
        <button>New Chat</button>
    </form>
    <br>

   <% for(let chat of chats){%>
        <div class="chat">
            <p>From:<i><%=chat.from%> </i></p>
            <div class="msg">
            <p><%=chat.msg%></p>
            <form action="/chats/<%=chat._id%>/edit" method="get">
            <button>edit</button>
            </form>
            </div>
            <p>Received by <i><%=chat.to %></p>
            <hr>
            <p><%=chat.created_at.toString().split(" ")[4]%></i></p>

            <p><%=chat.created_at.toString().split(" ").slice(0,4).join("-")%></i></p>

        </div>
        
        <br> <br>
    <%}%>

</body>
</html>

<!-- date me se time and date seprete krna hai
 so 1 convert in string .tostring
 now split string for sepretion using split method
 split hm index ke basis pe krne wale hain means jha bhi space aa jayega wha se split ho jayega... and sara ek array me store hoga,.....
 to uske baad sb comma se sepret print hoga now index se access krenge time and date ko
time ke litye split and indexing
while date ke liye split and sfter slice methode  ka use krenge...

and unn charo sepret jo join krenge date ke day month year using join method..

join("") se space ke sath join hoga
join("-") hyfon ke sath join
 -->

